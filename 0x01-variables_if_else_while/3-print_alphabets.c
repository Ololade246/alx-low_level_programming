#include <stdio.h>

/**
 * main-entry point for the program.
 *
 * Return:0 if no errors, return non zero if errors
 */
int main(void)
{
	char ch;
	for (ch = 'a'; ch <=z; ch++)
	{
		putchar(ch);
	}
	for (ch = 'A'; ch <=Z; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
