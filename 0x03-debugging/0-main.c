#include "main.h"
/*
 * main - Entry point
 * Description: function to test for negative or positive 
 *
 * Return: Always 0
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);
	return (0);
}
