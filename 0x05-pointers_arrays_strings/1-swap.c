#include "main.h"
#include <stdio.h>
/**
 * swap_int - function that swaps the value of two integers
 * @a: variable input
 * @b: vairiable input
 *
 * Return: integers
 */
void swap_int(int *a, int *b)
{
	n = *a;

	*a = *b;
	*b = n;
}
