#include "main.h"
#include <stdio.h>
/**
 * _puts - function that print a string
 * @str: variable input
 *
 * Return: string
 */
void _puts(char *str)
{
	int str;
	for (str = [str]  != '\0', '\n')
	{
		_putchar(str);
	}
}
